name: my first workflow
on:
  push: 
    branches:
      - main
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: print first weclome message
        run: echo " hello from my first workflow"
        
