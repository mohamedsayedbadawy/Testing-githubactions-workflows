function greet(name) {
    return `Hello, ${name}!`;
}

module.exports = greet;
//
// If run directly, print output (useful for debugging)
// (require.main === module) {
   //onsole.log(greet("World"));
//